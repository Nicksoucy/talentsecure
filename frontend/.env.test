# Variables d'environnement chargées par Vitest (mode "test"), committées exprès
# (contrairement à .env qui est gitignoré). Fixe l'URL d'API pour que les
# services et leurs mocks MSW visent la même base, en local ET en CI.
VITE_API_URL=http://localhost:5000
